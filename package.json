{
  "scripts": {
    "build": "python3 -m venv .pygbag-venv && . .pygbag-venv/bin/activate && python -m pip install -r requirements.txt && python -m pygbag --build --ume_block 0 --template static/sudoku.tmpl ."
  }
}
